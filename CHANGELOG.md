# Changelog

## 0.1.0
- Repo secrets scanning (regex rules + optional entropy heuristic)
- Web misconfiguration scanning (--url)
- Markdown + JSON reports
- .aminscan.yml configuration support
- GitHub Actions workflow + artifacts
- Unit tests
